import { FixedPositionDirective } from './fixed-position.directive';

describe('FixedPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedPositionDirective();
    expect(directive).toBeTruthy();
  });
});
